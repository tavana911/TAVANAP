# TODO

## Step 1
- Update `vite.config.js` to exactly match the provided snippet (remove extra imports/alias config; keep `@vitejs/plugin-react`).

## Step 2
- Update `package.json` `devDependencies` to match exactly the provided list.

## Step 3
- Run `npm install --legacy-peer-deps`.

## Step 4
- Run a quick check: `npm run dev` (or `npm test`) to ensure config works.

